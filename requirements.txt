# ============================================================
# FaceX — AI Face Recognition & Attendance System
# requirements.txt
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# NOTE: face_recognition requires dlib which requires CMake.
#   Linux:   sudo apt install cmake build-essential
#   Fedora:  sudo dnf install cmake gcc-c++
#   Windows: install CMake from https://cmake.org/download/
#            then: pip install dlib face_recognition
#
# NOTE: For GPU support replace torch/torchvision lines with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# ============================================================

# ── Core Computer Vision ──────────────────────────────────
  					 # Frame capture, resize, colour conversion, drawing
 opencv-python>=4.8.0                    # Use opencv-python instead if you need GUI windows from cv2

# ── Face Recognition (dlib-based) ────────────────────────
face_recognition>=1.3.0         # dlib ResNet-34 128-d encoding + HOG face detection
imutils>=0.5.4                  # Image path utilities used in encoder.py

# ── Deep Learning — Anti-Spoofing & Mask Detection ───────
torch>=2.0.0                    # MiniFASNet inference (CPU); MobileNetV2 training/inference
torchvision>=0.15.0             # MobileNetV2 pretrained weights; image transforms

# ── Data Handling ─────────────────────────────────────────
numpy>=1.24.0                   # Array operations, distance computation
pandas>=2.0.0                   # CSV read/write for attendance log

# ── Excel Export ──────────────────────────────────────────
openpyxl>=3.1.0                 # .xlsx creation with embedded face thumbnail images
Pillow>=10.0.0                  # Face snapshot thumbnail generation for Excel embedding

# ── Analytics & Metrics ───────────────────────────────────
matplotlib>=3.7.0               # Attendance charts (bar, pie)
scikit-learn>=1.3.0             # Confusion matrix, accuracy, precision, recall, F1

# ── GUI Dashboard ─────────────────────────────────────────
customtkinter>=5.2.0            # Modern dark-theme Tkinter wrapper for the dashboard
